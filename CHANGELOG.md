# Changelog

All notable changes to the Time Attendance System will be documented in this file.

## [1.2.7] - 2026-06-10
### Fixed
- **管理画面における労働時間「NaN」表示バグの修正**: 先のアップデートで公開用の `get_users` API から機密情報（休憩時間や基本勤務時間など）を削除した影響により、管理画面側で実労働時間や残業時間が計算できず `NaN時間 NaN分` と表示される不具合を修正。管理画面専用のセキュアなエンドポイント `get_admin_users` を新設し、管理者パスワード付きのリクエストにのみ全従業員データを返すようアーキテクチャを改善。


## [1.2.6] - 2026-06-10
### Fixed
- **出退勤ボタンの遅延・ハング改善**: `script.js` にて、ユーザー切り替え時のGASへの通信にタイムアウト(8秒)と非同期キャンセル(`AbortController`)を実装。ローカルキャッシュ(`ta_users_v1`)を用いて即座にボタン状態を反映することで、通信不調時に打刻ボタンが押せなくなるハングアップ問題を完全に解消。
- **サーバー側の二重打刻防止の強化**: `gas/Code.gs` にて、`LockService` を用いてトランザクションを保護し、サーバーサイドで現在の打刻ステータスを確認して不正な重複打刻（二重出勤や二重退勤）を確実にブロックするフェールクローズド設計を導入。
- **通信トラフィック・GAS実行時間の最適化**: `Code.gs` での打刻履歴検索を全行走査から逆順走査に変更し、当日より前のデータに到達した時点でループを終了。さらに `CacheService` を用いて従業員リストや打刻ステータスをキャッシュし、シート読み取り回数とレスポンス時間を大幅に改善。
- **通知メッセージ消去タイマーの競合修正**: 通知メッセージ（トースト表示）が複数連続で出た際、タイマーが競合してメッセージが一瞬で消えてしまう不具合を `clearTimeout` を用いて修正。

## [1.2.5] - 2026-06-02
### Fixed
- **管理画面全体のスクロール問題・左右中央寄せの修正**: 打刻画面用の `style.css` に定義されていた `body { overflow: hidden; }` および `justify-content: center` が管理画面でも影響し、縦長のテーブルをスクロールできなかった問題や、画面が左寄りに偏って右側に過剰な余白が発生していた問題を、`body` スタイルの強力なオーバーライド（縦スクロール許可、中央配置、上下マージンの確保）により完全解消。
- **代理打刻ボタンの左サイドバー統合とはみ出し見切れの解消**: 個別ビューの右カラム上部で、CSV / Excel 出力ボタンと並んで右側にはみ出していた「出勤」「退勤」ボタンを、左側の「個人契約設定」サイドバー下部へ「管理者代理打刻」として再配置・美しく統合。これにより、右カラムの表示領域が広がり、テーブルの窮屈さとはみ出し見切れバグを完全に解消。

## [1.2.4] - 2026-06-02
### Fixed
- **管理画面におけるモーダル無防備露出バグの完全修正**: `admin.html` において CSS 末尾の構文エラー（重複箇所と `</style>}` によるスタイル破壊）、および基礎スタイルシートである `style.css` のロード欠落が重なり、`.hidden` クラス（`display: none !important;`）が機能せず、「日別勤務データの編集」モーダルがロード時にいきなり全画面表示されて操作不能になっていた致命的なビジュアルバグを完全解消。

## [1.2.3] - 2026-06-02
### Fixed
- **管理ダッシュボードのログイン不能バグの修正**: `admin.html` で `script.js` をロードした際、打刻画面用の要素（`date-display`, `time-display`, `user-select`）が存在しないことによって JavaScript がクラッシュし、後続の `admin.js`（ログイン処理等）の読み込みが停止していた不具合を、要素有無のガード節（`if (!el) return;`）を追加することで完全に解消。

## [1.2.2] - 2026-06-02
### Fixed
- **HTML構造の破綻と二重化の完全修復**: 個別ビュー（`#individual-view`）内で発生していたHTMLタグの破損・二重化（CSV出力、代理打刻ボタン、サマリーカードなどの多重記述）を完全に排除し、正しい左右2カラムのプレミアムグリッド構成に修復。
- **styleタグの誤字修正**: `admin.html` 内の `<li    <style>` という崩れた style タグを正しい `<style>` へ修正。

### Changed
- **「プレミアムエメラルドグリーン」UIテーマへの大刷新**: 医療・薬局向けにふさわしい、みずみずしさと信頼感を両立したディープティールとミントグリーンを基調とするプレミアムUIデザインを適用。
- **ゴーストボタンの導入**: テーブル内のアクションボタン（編集、削除、承認、差戻し）の視認性を飛躍的に高めるため、コントラスト比の最適化と淡いミント/ローズ/エメラルドを背景とした「ゴーストスタイル」のボタンデザインへ変更。
- **アニメーションの追加**: ボタン押下やホバー時の滑らかなマイクロアニメーション（トランジションや縮小拡大）およびモーダル表示時のフェードインエフェクトを適用。
- **日曜・祝日および土曜の行背景の調和**: カレンダー勤務表での日曜・祝日（ソフトレッド）と土曜（ソフトブルー）の行背景を、プレミアムテーマに調和する淡く上品なトーンに変更。

## [1.2.1] - 2026-06-02
### Fixed
- **勤務表内の「編集」ボタンの視認性改善**: `.btn-small` クラスの文字色を白色（`color: white !important;`）に強制指定し、青背景に黒文字で文字が同化して見づらかった問題を完全に解消。
- **日付表示の短縮によるレイアウト調整**: テーブル内の日付フォーマットを `YYYY/MM/DD(曜日)` から `M/DD(曜日)` に短縮。これによりテーブル全体の幅がギュッとスッキリ収まり、横スクロールなしでも承認ボタンまで美しく表示されるよう改善。

## [1.2.0] - 2026-06-02
### Added
- **従業員個別詳細勤務表（出勤簿カレンダー）の導入**: 選択した従業員の1日〜末日までの全日の打刻開始・終了・実働・時間外をカレンダー形式で一覧表示し、土日祝日の色分け強調をサポート。
- **個人契約条件設定機能の追加**: 各従業員個別のメールアドレス、基本出勤・退勤時刻、休憩時間、月間所定労働時間、基本給料、勤務タイプを設定・変更できるサイドバーと保存機能を追加（GASデータベース「従業員」シートを自動拡張）。
- **日別勤務ステータスの有給/欠勤等の区分管理機能**: 日ごとに「出勤/有給/半給/欠勤/特別休暇」などの区分を設定できる機能を追加。
- **日別および月次の承認・差戻し機能**: 管理者がワンクリックで日ごとの勤務状況を「承認」または「差戻し」できる状態管理機能を追加（GASデータベースに「日別勤務データ」シートを自動新設）。
- **CSVダウンロード機能の実装**: 表示されている該当月・該当従業員の出勤表全体（基本給や所定、日別実働、日報、承認ステータス）を、BOM付きUTF-8（Excel文字化け防止）で瞬時にCSVファイルとしてダウンロードできる機能を実装。
- **管理者による代理打刻機能**: 従業員の代わりにワンクリックで出勤・退勤を代理打刻・記録できるクイックボタンを追加。

## [1.1.1] - 2026-06-02
### Fixed
- **管理画面のレイアウト崩れの修正**: 親コンテナの `text-align: center` 継承によるセクション見出し `h3` の中央配置および長音記号「ー」の折り返し（縦書き化）バグを `text-align: left` の適用により解決。
- **ボタンのサイズ異常と重なりの解消**: `.btn` に設定されていた `flex: 1` 継承の影響で「＋ 手動追加」ボタンや「追加 / 更新」ボタンが巨大化してヘッダーを覆っていたバグを、管理画面内の `.btn` に対して `flex: none` を適用することで完全に解消。
- **インプット要素のデザイン統一と美化**: 管理画面内の各種 input 要素から不要な `class="custom-select"` を削除し、管理者パスワードやメールアドレス等のインプットデザインを一元化・美化。

## [1.1.0] - 2026-06-02
### Added
- **管理ダッシュボードの統合**: 「月次集計ダッシュボード」と「システム管理画面」を1つの画面（`admin.html`）に統合しました。
- **打刻履歴の修正・追加・削除機能**: 管理画面から従業員の打刻履歴（時間、出退勤の種類）を直接編集し、GAS側のスプレッドシートに反映する機能を追加しました。
- **総滞在時間の表示**: 集計テーブルに、休憩時間を控除する前の「総滞在時間」を表示する列を追加し、労働時間計算の透明性を向上させました。
- `admin.js` 内に集計ロジック（祝日判定・自動休憩控除）を統合し、API通信を最適化しました。

### Changed
- `index.html` のフッターリンクを整理し、「管理ダッシュボード」への単一リンクに変更しました。

### Removed
- `dashboard.html` および `dashboard.js` を削除（`admin.html` および `admin.js` に統合したため）。

## [1.0.0] - 2026-06-02
### Added
- プロジェクトの初期リリース。
- QRコード読み取りによる出退勤打刻機能。
- Google Apps Script (GAS) 連携によるスプレッドシートへの記録機能。
- 祝日API連携および従業員ごとの自動休憩控除ロジック。
- 管理者認証によるダッシュボード機能。
